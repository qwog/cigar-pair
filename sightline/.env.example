# Copy to .env.local before running.
# 32+ byte random string used to sign session cookies and CSRF tokens.
SIGHTLINE_SECRET="change-me-openssl-rand-hex-32"
# SQLite by default. Point at a Postgres URL to swap the driver (see src/db/index.ts).
DATABASE_URL="file:./data/sightline.db"
